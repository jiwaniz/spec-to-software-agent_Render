# {{ app_name }}

Generated by Spec-to-Software Agent. Domain: **{{ domain }}**.

## Setup

```bash
pip install -r requirements.txt
uvicorn main:app --reload
```

Interactive API docs will be available at `http://127.0.0.1:8000/docs`.

{% if auth_enabled %}
## Authentication

This project includes a demo JWT auth scaffold. Get a token via:

```
POST /token?username=admin&password=admin
```

Then pass it as `Authorization: Bearer <token>` on protected endpoints.

**This is demo-only** -- replace the hardcoded credentials before any real use.
{% endif %}

## Running tests

```bash
pytest
```

## Endpoints

{% for ep in all_endpoints %}
- `{{ ep.method }} {{ ep.path }}` -- {{ ep.description }}
{% endfor %}

## Limitations

This is a prototype generated for a small CRUD use case. It does not include
authentication beyond the demo scaffold (if enabled), migrations, rate
limiting, or production-grade error handling.
